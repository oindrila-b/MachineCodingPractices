package Splitwise.SplitwiseOptimized.splitService.implementations;

import Splitwise.SplitwiseOptimized.splitService.CalculateSplit;
import Splitwise.SplitwiseOptimized.user.User;

import java.util.Map;

public class CalculateEqual implements CalculateSplit {
    @Override
    public void calculate(Long totalExpense, Map<User, Long> userOwedAmountMap, User spender) {

    }
}
